export interface Services {
  _id: string;
}
export interface ServiceProduct {
  _id: string;
  serviceDate: string;
  personId: string;
  productId: string;
  product: string;
  price: number;
  stock: number;
  }
